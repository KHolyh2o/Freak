# Freak 맵 제작 가이드 (Map CSV Guide)

이 문서는 Freak 프로젝트의 스테이지 맵(CSV)을 절차적으로 생성하거나 수동으로 디자인할 때 반드시 지켜야 하는 규칙을 정의합니다. AI(LLM)에게 맵 생성을 요청할 때 이 문서를 프롬프트로 제공하면 완벽한 형태의 맵을 얻을 수 있습니다.

## 1. 맵 파일 기본 구조 (CSV Format)
- 맵 파일은 `.csv` 확장자를 사용하며, 첫 줄은 반드시 아래의 헤더(Header)를 포함해야 합니다.
  ```csv
  x,z,y,type,extraOption,cost,tags
  ```
- **x, z, y:** 블록의 3D 공간 좌표입니다. (기본 바닥은 y=0)
- **type:** 블록의 종류 (`Start`, `End`, `Floor`, `Tree`, `Box`, `Stone`, `Puzzle`)
- **extraOption:** 특수한 속성을 정의합니다. (주로 Start/End의 바라보는 방향 각도)
- **cost:** 스테이지 클리어에 허용되는 최대 커맨드 개수 (Start 블록에만 기입)
- **tags:** AI가 부족한 스킬을 파악하기 위한 기믹 태그 (Start 블록에만 기입)

## 2. 블록 배치 핵심 규칙 (CRITICAL RULES)

### 2.1 필수 블록
- 모든 맵에는 **반드시 1개의 `Start` 블록과 1개의 `End` 블록**이 존재해야 합니다.

### 2.2 장애물의 중첩 배치 (매우 중요)
- `Tree`, `Box`, `Stone` 등의 장애물 블록은 공중에 떠 있는 것이 아니라 **바닥(`Floor`) 위에 설치되는 개념**입니다.
- 따라서 CSV 파일 내에서 장애물은 **자신이 밟고 있는 바닥(`Floor`) 블록과 동일한 x, z 좌표를 가져야 합니다.** 
- 게임 내 렌더링 스크립트(`MapGenerator.cs`)가 맵을 그릴 때, 장애물 타입이면 자동으로 y좌표를 +1 올려서 바닥 위에 예쁘게 올려줍니다.
  
**올바른 장애물 배치 예시:**
```csv
1,2,0,Floor,,
1,2,0,Tree,,
```
*(1, 2 좌표에 바닥을 깔고, 그 위에 나무를 심음)*

### 2.3 방향 설정 (extraOption)
- `Start` 블록과 `End` 블록이 바라보는 방향은 `extraOption` 열에 숫자로 입력합니다.
- 허용되는 각도: `0`, `90`, `180`, `270`

## 3. 기획 의도 및 스킬 태그 (Tags)

스테이지가 요구하는 학습 목표(코딩 기초 능력)에 따라 7번째 열(`tags`)에 태그를 조합하여 작성합니다. (여러 개일 경우 파이프 `|` 사용)

### 지원되는 태그 목록
- **`Sequence`:** 기본적인 순차적 실행 능력 (1~5 스테이지 위주)
- **`Spatial`:** 좌우 회전 및 방향 감각 (추락 방지)
- **`Function`:** 동일한 패턴을 함수로 묶는 능력 (계단식 맵 등)
- **`While`:** 긴 직선 등 노가다를 피하는 반복문 능력
- **`If`:** 장애물이 등장하여 상황에 맞게 회피해야 하는 조건문 능력
- **`ObstacleAvoidance`:** 장애물 회피 개념 자체 (보통 If와 묶임)

## 4. 올바른 맵 생성 예시 (Example)

**스테이지 컨셉:** 앞을 가로막는 돌(Stone)을 피해 우회해서 도착하는 맵 (조건문 필요)
```csv
x,z,y,type,extraOption,cost,tags
0,0,0,Start,90,10,ObstacleAvoidance|If
1,0,0,Floor,,
2,0,0,Floor,,
2,0,0,Stone,,
2,1,0,Floor,,
3,1,0,Floor,,
3,1,0,End,,
```
*해석: (0,0)에서 동쪽(90도)을 보고 시작. (2,0) 위치의 바닥 위에 돌(Stone)이 있어서 직진할 수 없음. 플레이어는 (1,0)에서 좌회전하여 우회해야 함.*
